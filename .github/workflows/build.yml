name: Build

on:
  push:
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Show Swift/Xcode version
        run: swift --version
      - name: swift build (macOS)
        run: swift build -v
